spring:
  test:
    database:
      replace: none
  init:
    mode: always
  datasource:
    url: jdbc:h2:mem:db;NON_KEYWORDS=VALUE
  liquibase:
    change-log: changelog_H2.sql
  mail:
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true
    host: smtp.gmail.com
    username: jira4jr@gmail.com
    password: zdfzsrqvgimldzyj
    port: 587